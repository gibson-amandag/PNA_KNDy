### Template Module

# https://shiny.rstudio.com/articles/modules.html

templateUI <- function(
  id
){
  ns <- NS(id)
  tagList(
    
    
  )
}


templateServer <- function(
  id
){
  moduleServer(
    id,
    function(input, output, session) {
      
      
    }
  )
}

